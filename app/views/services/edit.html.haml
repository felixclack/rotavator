= error_messages_for :service

- form_for(:service, :url => location_service_url(@location, @service), :html => { :method => :put }) do |f|
  = render :partial => "form", :locals => {:f => f}
  %p= submit_tag "Update"