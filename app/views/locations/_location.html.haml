%div[location]
  %p.name
    %strong Name
    = h location.name

  = link_to 'Show', object_path(location)
  |
  = link_to 'Edit', edit_object_path(location)
  |
  = link_to 'Destroy', object_path(location), :confirm => 'Really destroy location?', :method => :delete
