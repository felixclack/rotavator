%h1 
  Creating participation for 
  = @user.name

= error_messages_for :participation

- form_for(:participation, :url => user_participations_url(@user)) do |f|
  = render :partial => "form", :locals => {:f => f}
  %p= submit_tag "Create"