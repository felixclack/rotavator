%h1.overview_h1
  .floatr.btn_container
    %span.btn.btn-medium-white= link_to "Cancel", dashboard_path
  = title("Select a service", :span)

#overview-container
  = error_messages_for :rota
  - form_for(@rota) do |form|
    .overview.overview-top-left
      #locations
        - fields_for(@location) do |location|
          %p
            = location.label :id, "Location"
            = location.collection_select :id, Location.all, :id, :name, {:prompt => "Select a location..."}
    
      #services
    
      #formats
    
    #rota